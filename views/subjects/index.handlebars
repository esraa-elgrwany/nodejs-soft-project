<div class="container col-6">


   
    
    <div class="text-center text-center ">
        <br>
          <a class="btn btn-outline-dark" href="/subjects/create"><h1>add new subjects</h1></a>  </div>
        <br>

         <div class="card">
            <div class="card-body">
                <table class="table">
                 <tr>
                    <th>name</th>
                    <th></th>
                 </tr>
                {{#each subjects}}
                <tr class="d-flex justify-content-between">
               <th>{{name}}</th>
               
             <th><a class="btn btn-outline-success" href="/subjects/{{_id}}">show</a></th>
            
             <th><a class="btn btn-outline-success" href="/subjects/{{_id}}/edit">edit</a>
             </th>
             <th>
             <form action="/subjects/{{_id}}?_method=DElETE" method="post">
                <button type="submit"> Delete</button>
             </form>
             </th>
        
                </tr>
                {{/each}}
            </table>
        </div>
    </div>
</div>

    